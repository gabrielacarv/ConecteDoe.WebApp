@using ConecteDoe.Dominio.Entities

@model IEnumerable<PerfilInstituicaoViewModel>


<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Conecte Doe</title>
    <script src="https://kit.fontawesome.com/64d58efce2.js" crossorigin="anonymous"></script>
    <!--
      - favicon link
    -->
    <link rel="shortcut icon" href="https://raw.githubusercontent.com/gabrielacarv/ConecteDoeImagens/main/ConecteDoe.ico" type="image/svg+xml">

    <!--
      - custom css link
    -->
    <link rel="stylesheet" href="~/css/FeedInstituicao.css" asp-append-version="true" />
    <script src="script.js"></script>
    <!--
      - google font link
    -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbs5uP6N8rWi4qN6l5zg0y1W8WBRjISkYoYtBYb1L4pUZ5Jg6A/kwY8R+2DO2MMy" crossorigin="anonymous">

    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-maskmoney/3.0.2/jquery.maskMoney.min.js"></script>


    <script src="script.js" defer></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <div class="container">

            <a href="#" class="logo">
                <img src="https://raw.githubusercontent.com/gabrielacarv/ConecteDoeImagens/main/LogoConecteDoe.png" width="180px" alt="ConecteDoe logo">
            </a>
            <div class="navbar-wrapper">
                <button class="navbar-menu-btn" data-navbar-toggle-btn>
                    <ion-icon name="menu-outline"></ion-icon>
                </button>
                <nav class="navbar" data-navbar>
                    <ul class="navbar-list">
                        <li class="nav-item">
                            <a href="javascript:void(0);" onclick="goBack()" class="nav-link">Início</a>
                        </li>
                        <li class="nav-item">
                            <a asp-controller="Plataforma" asp-action="FeedInstituicao" class="nav-link">Instituições</a>
                        </li>

                        <li class="nav-item">
                            <a asp-controller="Logar" asp-action="Logout">
                                <svg id="IconSair" xmlns="http://www.w3.org/2000/svg" height="1.2em" viewBox="0 0 512 512">
                                    <!--! Font Awesome Free 6.4.2 by @@fontawesome - https://fontawesome.com License - https://fontawesome.com/license (Commercial License) Copyright 2023 Fonticons, Inc. -->
                                    <style>
                                        svg {
                                            fill: #abed1a
                                        }</style>
                                    <path d="M377.9 105.9L500.7 228.7c7.2 7.2 11.3 17.1 11.3 27.3s-4.1 20.1-11.3 27.3L377.9 406.1c-6.4 6.4-15 9.9-24 9.9c-18.7 0-33.9-15.2-33.9-33.9l0-62.1-128 0c-17.7 0-32-14.3-32-32l0-64c0-17.7 14.3-32 32-32l128 0 0-62.1c0-18.7 15.2-33.9 33.9-33.9c9 0 17.6 3.6 24 9.9zM160 96L96 96c-17.7 0-32 14.3-32 32l0 256c0 17.7 14.3 32 32 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32l-64 0c-53 0-96-43-96-96L0 128C0 75 43 32 96 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32z" />
                                </svg>
                            </a>
                        </li>

                        @* <a asp-controller="Logar" asp-action="Index" class="nav-link">
                        Sair
                        </a> *@
                    </ul>

                    @* <a asp-controller="Logar" asp-action="Index"><button class="btn btn-primary">Sair</button></a> *@
                </nav>
            </div>
        </div>
    </header>


    <section>
        <div class="container">
            <div class="titulo">
                <h2>Instituições</h2>
            </div>
            <div class="container-busca">
            <form id="searchForm" asp-controller="Plataforma" asp-action="FeedInstituicao" method="get">
                <input type="text" name="searchTerm" placeholder="Pesquisa" />
                <button type="submit">Buscar</button>
            </form>
            </div>
           
            <div class="card-container">
                @foreach (var item in Model)
                {
                    if (item.Instituicao != null && item.DadosInstituicao != null && item.Endereco != null)
                    {
                        <div class="card-conteudo">
                            <div class="card-dados-principal">
                                @*<img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcT85VbZ8wHdLOLkr2OyuBOOZ4WgeFMMvORLejAuM_WHipG5GMNGzL8V-a5F7I2L9PUQlSI&usqp=CAU">*@
                                <div class="card-dados-principal-superior">
                                    <img src="data:image/png;base64,@Convert.ToBase64String(item.Instituicao.Imagem)" alt="Imagem do Usuário" />
                                    <div class="texto-superior">
                                        <h1>@item.Instituicao.RazaoSocial</h1>
                                        <p>@item.DadosInstituicao.Categoria</p>
                                        <div class="localidade">
                                            <svg xmlns="http://www.w3.org/2000/svg" height="1em" viewBox="0 0 384 512"><!--! Font Awesome Free 6.4.2 by @@fontawesome - https://fontawesome.com License - https://fontawesome.com/license (Commercial License) Copyright 2023 Fonticons, Inc. --><path d="M215.7 499.2C267 435 384 279.4 384 192C384 86 298 0 192 0S0 86 0 192c0 87.4 117 243 168.3 307.2c12.3 15.3 35.1 15.3 47.4 0zM192 128a64 64 0 1 1 0 128 64 64 0 1 1 0-128z" /></svg>
                                            <p>@item.Endereco.Cidade-@item.Endereco.Estado</p>
                                        </div>
                                    </div>
                                </div>
                                <div class="botoes-card">
                                    <a class="openModalBtn" asp-controller="Instituicao" asp-action="PerfilInstituicaoExibicao" asp-route-id="@item.Instituicao.InstituicaoId">Visitar Perfil</a>
                                    @*<a asp-controller="PerfilGaleriaInstituicao" asp-action="PerfilGaleriaInstituicao"><button class="btn btn-secondary" desability>Visitar Perfil</button></a>*@
                                    <button class="openModalBtn" data-modal-id="@item.Instituicao.InstituicaoId" asp-route-id="@item.Instituicao.InstituicaoId">Fazer Doação</button>
                                </div>
                            </div>
                            <div id="myModal-@item.Instituicao.InstituicaoId" class="modal">
                                <div class="modal-content">
                                    <span class="close" data-modal-id="@item.Instituicao.InstituicaoId">&times;</span>
                                    <div class="card-dados-principal-modal">
                                        <img src="data:image/png;base64,@Convert.ToBase64String(item.Instituicao.Imagem)" alt="Imagem do Usuário" />
                                        <div class="texto-superior">
                                            <h1>@item.Instituicao.RazaoSocial</h1>
                                            <p>@item.DadosInstituicao.Categoria</p>
                                            <div class="localidade">
                                                <svg xmlns="http://www.w3.org/2000/svg" height="1em" viewBox="0 0 384 512"><!--! Font Awesome Free 6.4.2 by @@fontawesome - https://fontawesome.com License - https://fontawesome.com/license (Commercial License) Copyright 2023 Fonticons, Inc. --><path d="M215.7 499.2C267 435 384 279.4 384 192C384 86 298 0 192 0S0 86 0 192c0 87.4 117 243 168.3 307.2c12.3 15.3 35.1 15.3 47.4 0zM192 128a64 64 0 1 1 0 128 64 64 0 1 1 0-128z" /></svg>
                                                <p>@item.Endereco.Cidade-@item.Endereco.Estado</p>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="card-geral-secundario">
                                        <div class="card-dados-secundarios-modal">
                                            <h4>Chave Pix: @item.DadosInstituicao.ChavePix</h4>
                                            <label for="realInput">Digite o valor para doação: </label>

                                            @*<form id="formDoacao" asp-controller="Plataforma" asp-action="RealizarDoacao" class="btn-modal" method="POST" enctype="multipart/form-data" onsubmit="return enviarFormulario()">
                                            <div class="card-dados-secundarios-modal">
                                                    <div class="input-field">

                                                    <input type="text" name="valor" id="valor" class="realInputClass" required>
                                                    </div>
                                                    <div class="btn-modal">
                                                        <input type="hidden" name="id" value="@item.Instituicao.InstituicaoId" />

                                                        <label for="arquivo" id="input" required>Enviar Comprovante</label>
                                                        <input type="file" name="arquivo" id="arquivo" >
                                                        <button class="btn">Doar</button>
                                                    </div>
                                                </div>
                                        </form>*@

                                            <form id="formDoacao-@item.Instituicao.InstituicaoId" asp-controller="Plataforma" asp-action="RealizarDoacao" class="btn-modal" method="POST" enctype="multipart/form-data" onsubmit="return enviarFormulario('@item.Instituicao.InstituicaoId')">
                                                <div class="card-dados-secundarios-modal">
                                                    <div class="input-field">
                                                        <input type="text" name="valor" id="valor-@item.Instituicao.InstituicaoId" class="realInputClass" required>
                                                    </div>
                                                    <div class="btn-modal">
                                                        <input type="hidden" name="id" value="@item.Instituicao.InstituicaoId" />
                                                        <label for="arquivo-@item.Instituicao.InstituicaoId" id="input">Enviar Comprovante</label>
                                                        <input type="file" name="arquivo" id="arquivo-@item.Instituicao.InstituicaoId" required>
                                                        <button class="btn">Doar</button>
                                                    </div>
                                                </div>
                                            </form>

                                        </div>
                                        <div class="card-dados-secundarios-modal-2">
                                            <h4>Como Doar?</h4>
                                            <p>1 - Insira o valor que deseja doar.</p>
                                            <p>2 - Faça o PIX pelo seu banco de preferência.</p>
                                            <p>3 - Insira o comprovante de realização do PIX.</p>
                                            <p>4 - Clique em "Doar" para finalizar a doação.</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="card-dados-secundarios">
                                <h4>Causa</h4>
                                <div class="texto-limitado">
                                    <p class="texto-Para-Limitar">@item.DadosInstituicao.Causa</p>
                                </div>
                            </div>

                        </div>
                    }
                }
            </div>
        </div>

        @*<div class="pagination">
        @for (int i = 1; i <= ViewBag.TotalPaginas; i++)
        {
        <a href="@Url.Action("FeedInstituicao", new { pagina = i, itensPorPagina = ViewBag.ItensPorPagina })">
        @i
        </a>
        }
        </div>*@




        <div class="container-pagination">
            @*<button class="button" id="startBtn" disabled>
            <i class="fa-solid fa-angles-left"></i>
            </button>
            <button class="button prevNext" id="prev" disabled>
            <i class="fa-solid fa-angle-left"></i>
            </button>*@
            <div class="links">
                @for (int i = 1; i <= ViewBag.TotalPaginas; i++)
                {
                    <a href="@Url.Action("FeedInstituicao", new { pagina = i, itensPorPagina = ViewBag.ItensPorPagina })" class="link @(i == ViewBag.PaginaAtual ? "active" : "")">
                        @i
                    </a>
                }
            </div>
            @*<button class="button prevNext" id="next">
            <i class="fa-solid fa-angle-right"></i>
            </button>
            <button class="button" id="endBtn">
            <i class="fa-solid fa-angles-right"></i>
            </button>*@
        </div>






    </section>



    <script>

        function enviarFormulario(instituicaoId) {
                return true;
        }

        document.addEventListener('DOMContentLoaded', function () {
            // Limite de caracteres para texto
            var elementosTexto = document.querySelectorAll('.texto-para-limitar');

            elementosTexto.forEach(function (elemento) {
                var textoCompleto = elemento.textContent;
                var limiteCaracteres = 365; // Defina o número desejado de caracteres

                if (textoCompleto.length > limiteCaracteres) {
                    var textoExibido = textoCompleto.substring(0, limiteCaracteres);
                    elemento.textContent = textoExibido + '...';
                }
            });

            document.getElementById('searchForm').addEventListener('submit', function (e) {
                e.preventDefault();

                // Obter o termo de pesquisa do input
                var searchTerm = document.querySelector('input[name="searchTerm"]').value.toLowerCase();

                // Filtrar os cards com base no termo de pesquisa
                document.querySelectorAll('.card-conteudo').forEach(function (card) {
                    var cardText = card.textContent.toLowerCase();

                    // Exibir ou ocultar o card com base no termo de pesquisa
                    if (cardText.indexOf(searchTerm) !== -1) {
                        card.style.display = 'block';
                    } else {
                        card.style.display = 'none';
                    }
                });
            });


            ///// ENVIAR AS INFORMACAO PARA DOACAO
            //document.addEventListener("DOMContentLoaded", function () {
            //    var buttons = document.querySelectorAll(".openModalBtn");

            //    buttons.forEach(function (button) {
            //        button.addEventListener("click", function () {
            //            var instituicaoId = button.getAttribute("data-modal-id");
            //            enviarDadosDaDoacao(instituicaoId);
            //        });
            //    });

            //    function enviarDadosDaDoacao(instituicaoId) {
            //        // Aqui você pode fazer o que quiser com o instituicaoId, como enviar uma requisição AJAX
            //        // ou atualizar um formulário antes de enviar.
            //        // Exemplo: atualizar o action e o método do formulário de doação
            //        var form = document.querySelector(".btn-modal form");
            //        form.action = "/Controller/Action/" + instituicaoId;
            //        form.method = "POST";

            //        // Aqui você pode adicionar outros dados que deseja enviar junto com a doação, se necessário.

            //        // Agora, o formulário está configurado para enviar os dados corretos quando o usuário clicar em "Doar".
            //    }
            //});    
            
            //// Paginação
            // Selecting DOM elements
            const startBtn = document.querySelector("#startBtn"),
                endBtn = document.querySelector("#endBtn"),
                prevNext = document.querySelectorAll(".prevNext"),
                links = document.querySelectorAll(".link");

            // Setting an initial step
            let currentStep = ViewBag.PaginaAtual - 1; // Ajuste para começar na página correta

            // Function to update the button states
            const updateBtn = () => {
                startBtn.disabled = currentStep === 0;
                prevNext[0].disabled = currentStep === 0;
                endBtn.disabled = currentStep === links.length - 1;
                prevNext[1].disabled = currentStep === links.length - 1;

                // Remove the "active" class from all links
                links.forEach(link => link.classList.remove("active"));
                // Add the "active" class to the current page link
                links[currentStep].classList.add("active");
            };

            // Add event listeners to the number links
            links.forEach((link, index) => {
                link.addEventListener("click", (e) => {
                    e.preventDefault();
                    // Set the current step to the clicked number link
                    currentStep = index;
                    updateBtn(); // Update the button states
                });
            });

            // Add event listeners to the "Previous" and "Next" buttons
            prevNext.forEach((button, index) => {
                button.addEventListener("click", () => {
                    // Increment or decrement the current step based on the button clicked
                    currentStep += index === 1 ? 1 : -1;
                    updateBtn(); // Update the button states
                });
            });

            // Add event listener to the "Start" button
            startBtn.addEventListener("click", () => {
                currentStep = 0;
                updateBtn(); // Update the button states
            });

            // Add event listener to the "End" button
            endBtn.addEventListener("click", () => {
                currentStep = links.length - 1;
                updateBtn(); // Update the button states
            });

            // Initial button state setup
            updateBtn();


        });


        // Referências aos elementos DOM
        var modals = document.querySelectorAll('.modal');
        var openModalBtns = document.querySelectorAll('.openModalBtn');
        var closeModalBtns = document.querySelectorAll('.close');

        // Adicionar eventos de clique para abrir modais
        openModalBtns.forEach(function (btn) {
            btn.onclick = function () {
                var modalId = this.getAttribute('data-modal-id');
                var modal = document.getElementById('myModal-' + modalId);
                modal.style.display = 'block';
            };
        });

        // Adicionar eventos de clique para fechar modais
        closeModalBtns.forEach(function (btn) {
            btn.onclick = function () {
                var modalId = this.getAttribute('data-modal-id');
                var modal = document.getElementById('myModal-' + modalId);
                modal.style.display = 'none';
            };
        });

        // Fechar o modal se o usuário clicar fora dele
        window.onclick = function (event) {
            modals.forEach(function (modal) {
                if (event.target === modal) {
                    modal.style.display = 'none';
                }
            });
        };

        //$(document).ready(function () {
        //    // Adicionar evento ao formulário de busca
        //    $('form').submit(function (e) {
        //        // Impede o envio do formulário padrão

        //        // Obter o termo de pesquisa do input
        //        var searchTerm = $('input[name="searchTerm"]').val().toLowerCase();

        //        // Filtrar os cards com base no termo de pesquisa
        //        $('.card-conteudo').each(function () {
        //            var cardText = $(this).text().toLowerCase();

        //            // Exibir ou ocultar o card com base no termo de pesquisa
        //            if (cardText.indexOf(searchTerm) !== -1) {
        //                $(this).show();
        //            } else {
        //                $(this).hide();
        //            }
        //        });
        //    });
        //});

        function goBack() {
            window.history.back();
        }

        // Função para formatar o valor em Real
        $(document).ready(function () {
            $('.realInputClass').maskMoney({
                prefix: 'R$ ',
                allowNegative: false,
                thousands: '.',
                decimal: ',',
                affixesStay: false
            });
        });
    </script>
</body>   